import flatten from './core/flatten';
import {flip} from './core/Function';
import map from './core/map';
import {dec, divide, inc, multiple, negate, randomN, subtract, sum, sumArr} from './core/math';
import reduce from './core/reduce';

export { dec, divide, flatten, inc, map, multiple, negate, randomN, reduce, subtract, sum, sumArr , flip};
